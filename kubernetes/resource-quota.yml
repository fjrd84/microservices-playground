apiVersion: v1
kind: ResourceQuota
metadata:
  name: mem-cpu-quota
  namespace: rss-site-deployment
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 8Gi
    limits.cpu: "8"
    limits.memory: 16Gi
